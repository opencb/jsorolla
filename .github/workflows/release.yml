name: "Release"

on:
  push:
    tags:
      - "*"

jobs:
  build:
    uses: ./.github/workflows/build-jsorolla-workflow.yml

  test:
    uses: ./.github/workflows/test.yml
    needs: build
    secrets: inherit

  deploy-docker:
    uses: opencb/java-common-libs/.github/workflows/deploy-docker-hub-workflow.yml@develop
    needs: [ build, test ]
    with:
      repository: opencb/jsorolla
      branch: ${{ github.ref_name }}
      cli: python3 ./docker/docker-build.py push --tag ${{ needs.build.outputs.version }}
    secrets: inherit

  deploy:
    needs: [ build, test ]
    uses: ./.github/workflows/deploy-jsorolla-workflow.yml
    with:
      version: ${{ needs.build.outputs.version }}
    secrets: inherit

  release:
    needs: [build, test]
    uses: opencb/java-common-libs/.github/workflows/release-github-workflow.yml@develop
    with:
      artifact: jsorolla-release
      file: iva-${{ needs.build.outputs.version }}.tar.gz
